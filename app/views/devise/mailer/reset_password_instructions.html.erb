<p>こんにちは！ <%= @resource.name %>!</p>

<p>パスワードを変更するためには以下のリンクをクリックしてください。</p>

<p><%= link_to t("devise.passwords.edit.change_your_password"), edit_password_url(@resource, reset_password_token: @token) %></p>

<p>リクエストしていない場合は、このメールを無視してください。</p>
<p>上記のリンクにアクセスし、新しいパスワードを作成するまで、パスワードは変更されません。</p>
